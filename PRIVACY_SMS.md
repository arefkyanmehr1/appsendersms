# حریم خصوصی پردازش پیامک PayLink

## پردازش پیامک کاملاً محلی

پیامک بانکی برای تطبیق پرداخت روی خود دستگاه پردازش می‌شود.

- متن کامل پیامک به سرور PayLink ارسال نمی‌شود.
- اطلاعات حساس استخراج‌شده از پیامک مانند متن خام، شماره کارت و کد رهگیری در درخواست تأیید پرداخت ارسال نمی‌شوند.
- برنامه مبلغ قابل تشخیص از پیامک را با مبلغ فاکتورهای باز روی دستگاه تطبیق می‌دهد.
- برای تأیید پرداخت، فقط شناسه سفارش و مبلغ به API تأیید ارسال می‌شود.
- برای جلوگیری از تکرار درخواست، شناسه idempotency در هدر درخواست استفاده می‌شود و متن پیامک داخل بدنه درخواست قرار نمی‌گیرد.
- داده موقت پردازش پیامک پس از تعیین تکلیف موفق سفارش از دیتابیس محلی پاک می‌شود.
- دسترسی `READ_SMS` برای این نسخه استفاده نمی‌شود؛ برنامه فقط برای دریافت پیامک جدید از `RECEIVE_SMS` استفاده می‌کند.

## اعلان‌ها

اعلان سفارش جدید از همگام‌سازی فاکتورهای باز و اعلان تأیید پرداخت از نتیجه تأیید سرور ایجاد می‌شود. برای دریافت اعلان در پس‌زمینه، کاربر باید اجازه اعلان‌ها را فعال کرده باشد و محدودیت‌های باتری دستگاه اجازه اجرای پس‌زمینه را بدهد.

## ارتباط شبکه

برنامه برای عملیات PayLink مانند وضعیت حساب، فاکتورهای باز، heartbeat و نتیجه تأیید پرداخت به API متصل می‌شود. این ارتباط به معنی ارسال متن خام پیامک نیست.
